# Boxr - A lisp like programming language

## To run testing suite

`$ cargo test`

## To run language repl

`$ cargo run --bin=boxrepl`
`$ cargo run boxrepl`

## To run language binary

`$ cargo run --bin=boxr-cli <source-file>`
`$ cargo run boxr-cli <source-file>`
